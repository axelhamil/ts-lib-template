@AGENTS.md

<!-- This file is a pointer, not a source. -->

**Do not write here.** Every project instruction — a cross-cutting rule, a working
method, the session bootstrap, a change of tooling — belongs in `AGENTS.md`, which every
agent reads. Anything added below the import above is seen by Claude Code alone and
drifts from what the other agents are told, with nothing to signal it.

This applies to what Claude Code writes on its own: auto-memory, the `#` shortcut, and
any "note that down" during a session. Put it in `AGENTS.md`.
